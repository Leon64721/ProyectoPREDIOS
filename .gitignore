# gstack scratch (capturas de QA, reportes temporales)
.gstack/

# graphify — salida generada del grafo de conocimiento (graphify extract/tree/cluster-only)
graphify-out/

# gitleaks — artefacto de escaneo de secretos, no es código versionable
gitleaks-report.json

# Node.js — solo tooling de desarrollo (lint/husky/commitlint), no es el runtime
# de la app (eso es Google Apps Script/clasp) — node_modules nunca se despliega.
node_modules/
.husky/_/
npm-debug.log*
